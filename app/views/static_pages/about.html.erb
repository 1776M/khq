    <h1>About Us</h1>
    <p>
      The <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>
      is a project to make a book and screencasts to teach web development
      with <a href="http://rubyonrails.org/">Ruby on Rails</a>. This
      is the sample application for the tutorial.
    </p>

<% 
  require 'csv'

  thetable = CSV.table("public/assets/khq2.csv", :headers => true, :header_converters => :symbol)
%>

<%= thetable %>

<%   thetable = thetable.to_s %> <br/>
<% thetable = CSV.parse(thetable, :headers => true, :header_converters => :symbol) %>

<%
  lookup = Hash.new

  a = 0 
  thetable.each do |row|
    col_num = 0
    while col_num < thetable.headers().count
      lookup[row[0] + "." + thetable.headers[col_num].to_s] = row[col_num]
      
      col_num = col_num + 1
    end 
  end
  
%>
<b><%=thetable.headers[0] %></b>
<%=lookup %>


<%

a = [1,2,3]
b = 3
c = Array.new(4){ |x| x = b }

%>

<%= a %>

<%= c %>