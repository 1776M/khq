<%= provide(:title, 'Edit tab') %>
<h4>Edit tab</h4>

<div class="row">
  <div class="span12">

    <%= form_for @face do |f| %>
      <%= render 'shared/error_messages', :object => f.object %>

      <%= f.text_field :name %>

      <%= f.submit "Update", :class => "btn"  %>
    <% end %>
  </div>
</div>